<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>수능 합격 기원</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            text-align: center;
            padding: 50px;
            background-color: #f0f8ff;
            color: #333;
        }
        h1 {
            color: #2e8b57;
        }
        .message {
            font-size: 24px;
            font-weight: bold;
            margin-top: 20px;
        }
        .letter {
            font-size: 40px;
            font-weight: bold;
            color: #ff6347;
            animation: blink 1.5s infinite;
        }
        @keyframes blink {
            0% { opacity: 1; }
            50% { opacity: 0.5; }
            100% { opacity: 1; }
        }
        .celebration {
            font-size: 50px;
            color: #ff1493;
            margin-top: 30px;
        }
        .qr-code {
            margin-top: 50px;
            width: 150px;
            height: 150px;
            margin-left: auto;
            margin-right: auto;
        }
    </style>
</head>
<body>
    <h1>수능 합격을 기원합니다! 🎉</h1>
    <p class="message">합격의 기운을 보내드립니다... 잠시만 기다려주세요.</p>
    
    <div id="greeting" class="message"></div>

    <script>
        // 합격 메시지를 점진적으로 출력
        const 합격 = ["수", "능", "합", "격"];
        let index = 0;
        const greetingElement = document.getElementById("greeting");

        function showMessage() {
            if (index < 합격.length) {
                const letterElement = document.createElement("span");
                letterElement.classList.add("letter");
                letterElement.innerText = 합격[index];
                greetingElement.appendChild(letterElement);
                index++;
                setTimeout(showMessage, 500); // 0.5초마다 한 글자씩 출력
            } else {
                setTimeout(() => {
                    document.body.innerHTML += "<p class='celebration'>🎉🎉🎉 수능 합격 축하드립니다! 🎉🎉🎉</p>";
                    document.body.innerHTML += "<p>이제 프로그래밍처럼 문제를 해결하는 인생을 기대하세요! 🚀</p>";
                }, 500);
            }
        }

        // 메시지 출력 시작
        showMessage();
    </script>

    <div>
        <p>QR 코드로 확인하고 싶으세요?</p>
        <img class="qr-code" id="qrCode" src="" alt="QR Code" />
    </div>

    <script>
        // QR 코드 생성
        const qrCodeUrl = encodeURIComponent(window.location.href);
        document.getElementById("qrCode").src = `https://api.qrserver.com/v1/create-qr-code/?data=${qrCodeUrl}&size=150x150`;
    </script>
</body>
</html>
